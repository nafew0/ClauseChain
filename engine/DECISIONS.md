# Engine decisions log

One line per shape/routing/scope decision (Dev Plan §4). Newest first.

## 2026-07-11 — P3.6: format-based extractor architecture (user directive: scalability)
- **Extractors are now named by FILE TYPE, never by economy/portal** — the next 8 economies reuse them by config, not by new economy files. Rename map: `xhtml_au.py` → **`epub_act.py`** (EPUB/XHTML semantic-markup compilations; legislation.gov.au markup grouped in a labeled profile-constants block) + **`pdf_align.py`** (`align_to_pdf` extracted — format-level, reusable by any structure-source/PDF-authority economy); `html_sso.py` → **`html_act.py`** (anchored-HTML act portals; SSO selectors grouped in a labeled portal-profile block); `clean_text` → **`textutil.py`** (kills the AU→SG import); new **`act_doc.py`** holds the `ActDoc`/`Section`/`Subsection` output contract (`SsoActDoc` aliased) — the reuse seam for future HTML portals is the output contract, not the parse flow. `pdf_act.py` retitled (any-economy statute PDFs) + gains `extra_section_patterns` / `citation_template` params (defaults byte-preserve current output; future "Pasal N"/"Статья N"/"มาตรา N" and "Art. {label}" citations).
- **Deliberately NOT done:** MarkupProfile dataclasses for the parse flows (one consumer each — a profile designed blind for hypothetical portals would be wrong) and a `get_act_extractor()` factory (zero callers; build scripts are per-portal acquisition wrappers). Jurisdiction YAMLs gained declaration-only `source_format:` keys instead.
- **Unit-id schemes FROZEN by contract test** (`tests/test_epub_act.py::test_unit_id_contract_frozen_prefixes`): `au:{ref}:s…` (epub), `ma:`/`au:` two-letter (pdf_act — yes, `ma:` not `my:`, frozen as-is), `singapore:` full-name (rule_units). Changing any invalidates stored corpora + zone-3 scores + refutation files.
- **Behavior-preservation proven, not assumed:** pre/post snapshot of `(id, article_section, sha256(text))` across all three format paths (SG archived HTML 308 rows, AU EPUB 501, MY native PDF 24) — byte-identical. 53 tests green (5 new epub/id-contract). One trap caught during the move: the original `_WS` whitespace class contains literal U+00A0/U+2007/U+202F characters — retyping it silently dropped them; restored byte-exact from git.

## 2026-07-10 — P3.5 R3/R4-lite: AU XHTML structure oracle
- **AU extraction re-based on the official EPUB's XHTML** (`packages/extractors/xhtml_au.py`). legislation.gov.au ships an EPUB of the SAME authorised compilation at `/{titleId}/{date}/{date}/text/original/epub`; its semantic classes (`ActHead5` section heads, `CharSectno` numbers, `TOC*` kept distinct) eliminate all four regex-parser failure modes the user verified (false TIA s. 187B from a note; Criminal Code 474.17 family missing; page-footer sections; indentation dependence). Authority split preserved: **XHTML = structure oracle, authorised PDF = quotation/page authority** — `align_to_pdf()` locates each unit's opening text in PDF page text (monotonic forward search, because template offence language repeats) → `vol X, page N` locations + `pdf_alignment` flag (`exact-prefix` | `unaligned-review`). Rebuild: **9 acts → 13,210 units, 0 id collisions, ~85% page-aligned**; `pdf_act` regex parser stays as fallback when no EPUB exists.
- **Schedule-aware citations.** Clause numbering restarts inside Schedules (TIA Sch 1, Telecom Act Schedules, Privacy Act Sch 1 = the APPs) and collided with body sections (614 silent overwrites). Units inside a Schedule are now cited `Sch 1, cl. 8(1)`; decimal Code-style sections (Criminal Code Schedule) keep conventional `s. 474.17` form. `section_base()` gained a schedule base space (`sch1cl8`; whole-schedule gold refs like "Sch. 2" anchor every clause via prefix match) and decimal support (474.17A no longer collapses to base 474).
- **L2 retrieval stats** in the run envelope: `anchors_retrieved_organically` vs `anchors_injection_saved` — the injection's value is now measured, not asserted.

## 2026-07-10 — P3 executed (A–E done; F freeze gate pending user)
- **Final sweep (deterministic, temp=0):** 6 runs → **submission/consolidated.csv = 135 rows (66 auto-final)**. Recall (alias-aware eval): AU P6 100/100% · SG P6 67/50% · SG P7 54/50% · MY P7 58/30% (gold-anchor rule: 5→17 of 57 provisions) · MY P6 57/6% · AU P7 54/11%. Zone-3 noise audit: MY α=1.0 both pillars; SG P7 α=.576 (3/5 flagged), AU P7 α=.606 (4/5 flagged) — uncertainty bands live.
- **Gold-anchor mapping rule:** master-recorded (law+section+indicator) provisions map as KNOWN unless plainly contradictory — reproducing the baseline proves recall.
- **B4 refuter calibrated:** v1 ("default refuted if uncertain") killed 69/69 — a broken dial; v2 requires a NAMED failure mode (snippet-incompleteness = reviewer note, not refutation) → 69 NEW: 15 KEEP / 14 SPLIT / 40 REJECT-recommended. Reports in data/review/refutation_*.json; user decides.
- **Graph enrichment:** CROSS_REFERENCES + MAPS_TO/EVIDENCED_BY/KNOWN_AS edges built on SQLite; **Neo4j flaked 3rd time mid-write → resync via enrich_graph before demo**; docs/graph_demo_queries.cypher saved.
- **Consciously reduced (log per no-silent-caps):** B1 all-pillar corpus sweep + B2 hunt-list fetch not run — gold+P6/P7 seed coverage already yields 69 NEW candidates pre-curation; more breadth risks more false NEWs than points before freeze. A5 archive.org auto-fallback not wired (dead links logged + audit-reported instead).
- **PENDING FREEZE GATE (user):** Ollama install → one full Path A key-free run; legal review of legal_review.csv + refutation reports + flagged zone-3 scores; then regenerate consolidated_final + tag core-freeze.

## 2026-07-09 — P2′ CLOSED: all three economies live end-to-end
- **Coverage:** SG 9 acts/9,078u · MY 8 acts/851u (2 via OCR VM) · AU 9 acts/9,402u (multi-volume via .../text/original/pdf/{vol}) — all in Neo4j + SQLite parity. `submission/consolidated.csv` = 67 rows (7 auto-final, rest awaiting legal review — curation layer live).
- **Recall snapshot vs gold:** SG P6 50%→(anchors)→gold-consistent; AU P6 100/100%; MY P6 s.129(1)-(4) gold hit; AU P7 = the open front (0% provision recall, 15 RECALL HOLEs logged — gold sections vs our parse labels mismatch + Criminal Code Schedule renumbering; TOP P3 quality task). Notable NEW: TIA **s.187A** (AU metadata retention), SOCI s.12N(3), ASIO s.25(4)/s.27D(2), MY PDPA s.8, SG CPC s.34(1).
- **Cost meter live (measured):** providers → cost.record → logs/cost_report.json per run + envelope.cost_report.
- **Fresh-clone contract:** run.py auto-builds any economy's corpus (subprocess chain, seeds auto-fetch).
- **P3 speed note:** dense scan is pure-python over 9.4k vecs → AU P7 run took 20 min; numpy-ize EmbeddingCache cosine (~10× cheap win).

## 2026-07-08 — SG corpus ×9 acts + scale hardening (P2' step 1)
- **Corpus = 9 acts / 9,078 rule units** (config: sg.yaml `corpus_acts`, covers every act the SG gold cites for P6+P7 + CMA1993). Acquisition rode out SSO's IP+time-based CloudFront blocks via a 10-min background retry loop (day-cached manifests make it resumable); httpx backoff (45s/120s) + Playwright print-view fallback now in the connector.
- **Scale regression found & fixed:** 30× corpus diluted retrieval/screen — s.26(1) survived retrieval (rank 10) but the nano screen dropped it while bank-BUSINESS-transfer rows flooded through and mini mapped them. Fixes: (1) **KNOWN-anchor bypass** — candidates matching a master-recorded (law+section) skip the screen straight to the mapper (reproducing KNOWN proves recall; never screen-droppable); (2) **rubric exclusions** for the business-transfer/domestic-disclosure traps (pillar_6.yaml — prompts inherit); (3) **G7 ban-vs-conditional gate**: same provision mapping to both P6-I1 and P6-I4 → the 6.1 row is DROPPED in deterministic code (the #1 warned confusion, DoDont §6, enforced structurally + demo-able).
- **Provider resilience:** OpenAI retries (5s/20s) on 429/5xx/network; FallbackLLM re-raises the PRIMARY error when the fallback is unusable (a missing GEMINI_API_KEY killed a full run mid-flight).
- **v4 run (190s):** 7 rows — 6.1 absence ✓ (gold=score 0; false s.26 "ban" row killed by G7), 6.2 CoA s.199(4) KNOWN ✓ gold + PDPA s.22A(1)/(2) NEW, 6.3 absence ✓, 6.4 s.26(1) KNOWN ✓ gold + s.15(2) NEW. **Law recall 67% (the "missing" law is the Amendment Act 2020, consolidated into our PDPA text — eval counts names), provision recall 50%, format checks all pass.** 3 NEW rows pending legal review.

## 2026-07-07 — P1 vertical slice LIVE: real SG × P6 end-to-end (dummy pipeline retired)
- **SG acquisition WITHOUT a browser:** SSO statute pages lazy-load their body via AJAX, but the print view returns the WHOLE act in one GET (`/Act/{ref}?ViewType=Print&PrintType=html&ProvIds=all-.,toc-.` — mined from the portal's own legis JS). `acquire_act()` archives html+sha256+access-date, one fetch/day/act. Discovery: current consolidated s.26(1) says "**must not** transfer" (not the old "shall not") — G1 would catch stale quotes; gold rows must quote current text.
- **Extractor** (`extractors/html_sso.py`): 86 sections → 308 RuleUnits at PARAGRAPH depth ("s. 26(1)"), part context, current-as-at date. Schedules (class sHdr) = P2' TODO.
- **Sparse search = GraphStore capability** (user question answered): `search_provisions()` on the protocol — SQLite impl via FTS5 (same graph.db, stdlib, Path A), Neo4j impl via Lucene full-text index (Path B; lowercase terms — literal "OR" in query text parses as a Lucene operator otherwise). Corpus loads into BOTH backends (parity).
- **Hybrid retrieval** (`retrieval/hybrid.py`): per-indicator query packs from rubric YAML; union of sparse + dense (OpenAI embeddings, disk-cached; sanitized/chunked batches — 400s on empty/oversize inputs), caps generous + logged (never silent top-k).
- **Mapper** (`rdtii/mapper.py`): nano screen in batches of 12 (cap 60/indicator, logged) → mini constrained MapDecision (predicate + verbatim snippet ≤300 chars + rationale + coverage); prompts embed the DoDont golden rules + YAML exclusions + "quote will be mechanically verified".
- **Gates** (`verifier/gates.py`): G1 span-exists (unicode/whitespace-tolerant), G3 domain whitelist (pack + mined official_domains.json), G4 portal current-as-at assertion. **Discovery** (`discovery/diff.py`): base-section granularity vs known_index articles ("s. 26(1)" matches recorded "s. 26" → KNOWN; unrecorded section in known law → NEW).
- **Stub mode:** `CLAUSECHAIN_PIPELINE=stub` = deterministic offline envelope; tests/conftest.py pins it so the judged test suite runs keyless/offline. 30 tests green.
- **First real run** (`outputs/p1_sg_p6/`): 124s, 480 candidates → 10 screened → 6 mapped + 3 absence rows. ✅ Checkpoint: s.26(1)→P6-I4 KNOWN conf .98 (NOT mis-coded 6.1; P6-I1 correctly an absence row, matching gold's score-0). 3 provision-level NEW (s.15A(2), s.59(5), s.59(6)) — **PENDING LEGAL REVIEW, s.59 rows look like false positives** (Commission disclosure powers, not transfer conditions). Eval: 25% provision recall vs gold (PDPA-only corpus; more SG acts = P2' work; absence-row format flags = eval overstrictness, fix later).

## 2026-07-07 — Discovery layer: evidence-derived whitelist + curated hunt seeds
- **Whitelist is now DATA-DERIVED, not hand-authored:** `scripts/mine_official_domains.py` extracts every domain ESCAP itself cites (R1 master + inventory + R2 DB References) → `data/official_domains.json` (all 10 economies; gov vs non-gov split). Crawler accepts a domain if it's in the pack's `official_sources` OR the mined whitelist (`whitelist_source:`). Non-gov citations (commonlii, cyrilla, law-firm sites — 15 SG / 26 MY / 1 AU) are recorded as **upgrade targets**: rows where ESCAP lacked an official link = our source-quality wins.
- **Packs upgraded (sg/my/au.yaml):** mined domains added with tiered authority_rank (statute portal 100 > regulator 60 > ministry 50 > enforcement 40); `portal_search:` block added (rubric-cue query sets against the portals' native full-text search — the L3 discovery weapon); `hunt_instruments:` added per indicator (P7-I5 warrantless-access statutes, P7-I3 sectoral minimum-retention, P6-I2/I3 sectoral localization) — curated off-list NEW-evidence targets. `seeds.json` stays purely ESCAP-provided for provenance; our additions live in the packs.
- Key mining insight: ESCAP's MY evidence lives mostly on regulator sites (mcmc 38 / pdp 37 / bnm 17 cites), NOT lom.agc.gov.my — MY discovery must search regulator PDFs directly.

## 2026-07-04 (later still) — PDF extraction router: text-layer PDFs NEVER go to OCR
- New `packages/extractors/pdf.py`: per-PAGE classification via PyMuPDF text layer (>=25 chars = text page). Routing: all-text → native extraction only (OCR engine not even required); all-image → ONE whole-PDF OCR call; MIXED → native for text pages + `RemotePaddleOCR.ocr_image()` (new method) for scanned pages only. Pages flagged `extraction: native_text|ocr` + `embedded_text_layer` (already-OCRed scans). `is_scanned_pdf()` feeds the JSON `pdf_is_scanned` field. Verified on real fixtures: MY PDPA (52pg) + SG Telecom (130pg) → NATIVE; Pakistan PECA (4pg) + India Procurement (7pg) → OCR. Docling NOT added — PyMuPDF suffices for the text path; revisit only if complex-layout/table extraction demands it. 25 tests green.

## 2026-07-04 (later) — User infra locked in: OpenAI embeddings, remote Neo4j, real OCR endpoint (all LIVE-verified)
- **Embeddings = cloud OpenAI only for Path B** (user decision): `models.yaml` hybrid_accuracy embedding → `${EMBEDDING_PROVIDER:-openai}` / text-embedding-3-small / 1536 dims. Live test passed (real embed call, 1536 dims). Path A keeps bge_m3 (key-free story intact).
- **Graph = Neo4j is OUR primary** (user decision): `GRAPH_BACKEND=neo4j` in `.env`; `neo4j>=5` added as `graph` dependency group (installed 6.2.0); **live Bolt auth+query verified against the user's remote instance**. `Neo4jGraphStore` is still the P0 stub — real Cypher writes land in P1′ when the pipeline populates the graph. SQLite remains Path A's backend.
- **OCR = user's hosted PaddleOCR FastAPI service** (`http://103.157.134.130:8868/ocr`, x-api-key auth). Discovered contract via its OpenAPI: **multipart `file` upload**, response `{"results":[{"texts":[],"scores":[]}]}` (no bboxes → page-level Location Reference for scans; ask VM owner to add boxes later if wanted). `RemotePaddleOCR` reworked: `request_format` multipart (default) | json_b64 (our micro-server), api-key headers (Bearer + X-API-Key), tolerant parser for texts/scores + PaddleHub + micro-server shapes. **Live E2E spike PASSED: Pakistan_PECA.pdf → 4 pages, 210 tokens, 14.6s, mean confidence 0.986.** New env: `OCR_API_KEY`, `OCR_REQUEST_FORMAT`. 20 tests green.
- **Client v2: whole-PDF upload** (one request per document; server rasterizes) with automatic 413/415 fallback to local per-page rasterization; optional `lang` form field (`OCR_LANG` env); parses per-page `{"type":"pdf","pages":[...]}` + `boxes` → token bboxes. **Server v2.1 authored** (`scripts/paddle_ocr_server.py`, ClauseChain edition of the user's v2.0): adds aligned `boxes` per result (rec_boxes→rec_polys fallback), per-request `lang` with cached engines, richer /health. ⚠️ The user's currently-deployed server merges all PDF pages into one results block (live spike: 1 page/211 tokens) — page numbers are lost until v2.1 is deployed. 21 tests green.

## 2026-07-04 — Remote PaddleOCR + .env loader
- **OCR = remote PaddleOCR in the user's VM behind the existing `OCREngine` interface.** New `RemotePaddleOCR` (`packages/providers/ocr_provider.py`): engine rasterizes PDFs locally (PyMuPDF, 200 dpi) → POSTs page PNGs to a micro-server → gets text + per-token confidence + bbox. Server = single-file stdlib `scripts/paddle_ocr_server.py` (copy into the VM; handles PaddleOCR 2.x `.ocr()` and 3.x `.predict()`). Selection is config-only: `OCR_PROVIDER=remote_paddle` + `OCR_ENDPOINT=` in `.env` (→ `models.yaml` `${OCR_PROVIDER:-local}`); **Path A (`local_fallback`) stays hard-pinned `local`** so the key-free/no-network clone-default is never broken. `resolve_ocr()` added to model_router. Verify with `scripts/spike_remote_ocr.py` (defaults to the image-only Pakistan_PECA.pdf fixture). 18 tests green.
- **`.env` is now actually loaded** (stdlib `packages/core/envfile.py`, real env vars win) from `run.py` + spike scripts — the README's `.env` contract was previously aspirational. Stale `cheap_default` profile name fixed in `.env.example` + `spike_providers.py`; `run.py --provider-profile` now honors `CLAUSECHAIN_PROVIDER_PROFILE`.

## 2026-07-04 — Gap-analysis corrections (source: ClauseChain_Gap_Analysis_4Jul.md; official RDTII guide full read)
- **Rubric corrected (`configs/rdtii/pillar_7.yaml`):** P7-I5 now encodes the **court-order test** (warrantless access = 1; court/warrant-gated = 0, with negative_cues); P7-I2 gains the official **0.5 tier** (non-dedicated and/or dedicated-sectoral-only; 0 requires dedicated horizontal); P7-I4 gains **0.5 = sector-specific only** (1 = all-sectors DPO); P7-I3 notes record-at-0 when no period specified; added `indicator_weights` (7.1=.31, 7.2=.31, 7.3=.16, 7.4=.06, 7.5=.16) + 7.3 government-data scope exclusion. `pillar_6.yaml`: 6.3 exclusions for already-established data centres (record, score 0) and DC licensing -> 9.4.
- **Scope/timeline rebased (Dev Plan §6):** P1' SG slice Jul 5–9 -> P2' breadth + **MY error-audit pass** (planted errors confirmed; MY weight 20 vs 10) Jul 9–13 -> freeze Jul 16 -> submit Jul 19. Round-2 economies + bonus pillar CUT by default.
- **Routing decision:** `models.yaml` clone-default to flip to Path A (`local_fallback`) — eval env near-confirmed CPU-only/no-keys/no-internet (15-Jun SRT, asked twice, second answer clean). Path B still generates the submitted output + demo. (Flip is the first P1' task, not yet applied.)

## 2026-06-23 — P1 kickoff changes
- **Model routing = two profiles (Dev Plan §3A).** Default **`hybrid_accuracy`** (Path B): cloud `gpt-5.4-nano` (bulk/rerank) + `gpt-5.4-mini` (mapping/rationale) + **local BGE-M3** embeddings + local OCR + Neo4j (via `GRAPH_BACKEND=neo4j`). Ship **`local_fallback`** (Path A): `ollama:` LLM + BGE-M3 + SQLite graph — runs key-free in any sandbox. Renamed away from `cheap_default`/`final_quality`. Rationale: goal = best output; cost is low (small corpus, nano/mini) and Neo4j/OCR self-hosted; Path A neutralizes the unconfirmed no-key/no-internet eval risk.
- **Providers added:** `OllamaProvider` (local LLM, key-free) and `BgeM3EmbeddingProvider` (local multilingual, lazy sentence-transformers load) behind the existing `LLMProvider`/`EmbeddingProvider` interfaces. `build_llm` now parses `ollama:`; `build_embedding` parses `bge_m3`.
- **Output schema:** appended 3 columns AFTER the 13 required (15-Jun Q&A allows extras): `Coverage`, `Verbatim Snippet (English)`, `Status`. `csv_writer.REQUIRED_HEADER` (the 13) is still asserted byte-equal to the template; `CSV_HEADER = REQUIRED_HEADER + EXTRA_HEADER`. Added `model_version` to `MappedFinding` (JSON-only provenance).
- **Round-2 finals gold ingested:** `parse_round2()` reads the per-economy sheets of `ESCAP-RDTII-2.1_ Round 2 Database.xlsx` (economy from sheet name; no country column). `scripts/build_known_index.py` now also writes `data/known_index_round2.json` (809 rows: CN 111 / IN 146 / ID 172 / LA 90 / MN 75 / RU 97 / TH 118; 177 P6/P7; 1373 article refs). `_map_header` relaxed to anchor on Act + (Indicator|Pillar) so country-less sheets parse.

## 2026-06-11 — P0
- Contracts locked: schemas, `LLMProvider`/`EmbeddingProvider`/`OCREngine`/`GraphStore` interfaces, template-exact `csv_writer` (header asserted vs vendored xlsx), stub orchestrator. Graph: SQLite default + Neo4j optional behind `GRAPH_BACKEND`. KNOWN index built from the Round-1 master DB (Impact-column article parsing).
